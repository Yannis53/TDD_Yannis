class Book:
    def __init__(self, isbn, title, author, editor, format, is_available):
        self.isbn = isbn
        self.title = title
        self.author = author
        self.editor = editor
        self.format = format
        self.is_available = is_available   